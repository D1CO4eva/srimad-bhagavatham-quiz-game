/** Bounds for a question's per-question time limit, enforced both client- and server-side. */
export const MIN_TIME_LIMIT_SECS = 20;
export const MAX_TIME_LIMIT_SECS = 120;
export const DEFAULT_TIME_LIMIT_SECS = 20;
